name: "CodeQL"

on:
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * 6'

jobs:
  analyze:
    runs-on: windows-latest
    permissions:
      security-events: write
    timeout-minutes: 30
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: csharp

      - name: Setup DotNet Environment
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '6.0.x'

      - name: Restore
        run: dotnet restore ./SurgingCloud.Cli/SurgingCloud.Cli.csproj

      - name: Build
        run: dotnet build ./SurgingCloud.Cli/SurgingCloud.Cli.csproj -c Release --no-restore

      - name: Run CodeQL Analysis
        uses: github/codeql-action/analyze@v3